import React from 'react';
import './calendar-table.css';

type CalendarTable = {
    value: number
}

const CalendarTable = ({value}: CalendarTable) => {
    return (
        <ul>
        </ul>
    );
}

export default CalendarTable;
